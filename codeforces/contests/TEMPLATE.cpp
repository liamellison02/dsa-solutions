#include <bits/stdc++.h>
#define FOR(i,a,b) for (int i = a; i <= b; i++)
typedef long long ll;
typedef vector<int> vi;
typedef vector<vi> vvi;

void solution() {
    return;
}

int main() {
    int t;
    cin >> t;
    while (t--) solution();
    return 0;
}
